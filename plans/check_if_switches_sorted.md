Add a check to make sure that switches are sorted `are_switches_sorted()`

we use `sort_switch_texts` to sort them. but I need to check before if they are sorted or not.

if they are not we can fix them and upload new versoin to commons.

where can u put this?
